# === Segredos ===
.env
.env.*
!.env.example

# === Dados persistentes ===
# Caso VOLUMES_BASE aponte para dentro do repo em testes locais, não versione
# o conteúdo servido. Os placeholders versionados ficam em ./seed/.
published/
volumes/
data/

# === OS / Editores ===
.DS_Store
Thumbs.db
*.swp
