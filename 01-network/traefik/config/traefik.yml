log:
  level: DEBUG

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https

  websecure:
    address: ":443"
    http:
      tls:
        certResolver: "letsencrypt"
        domains:
          - main: "daberga.com"
            sans:
              - "*.daberga.com"
    # Configuração refinada: o Traefik agora confia apenas no seu gateway
    forwardedHeaders:
      trustedIPs:
        - "127.0.0.1/32"       # Localhost
        - "192.168.10.0/24"    # A rede interna (que já inclui o .1 do OPNsense)
        - "192.168.3.0/24"     # Rede local backup
providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: "bergatrix-proxy"

certificatesResolvers:
  letsencrypt:
    acme:
      storage: "/acme.json"
      # CA de produção por padrão (sem a linha caServer = produção).
      # Para TESTAR a emissão sem gastar o rate limit de produção, descomente o staging.
      # Certificados de staging NÃO são confiáveis no browser; ao voltar para produção,
      # apague o acme.json para reemitir certificados válidos.
      # caServer: https://acme-staging-v02.api.letsencrypt.org/directory
      dnsChallenge:
        provider: desec
        propagation:
          delayBeforeChecks: "30s"
        resolvers:
          - "ns1.desec.io:53"
          - "ns2.desec.org:53"
